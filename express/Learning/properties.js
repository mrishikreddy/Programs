module.exports = {
    DB_PORT : 4200,
    DB_URL : "mongodb+srv://malerishikreddy:rishikreddy2005@cluster0.cseo3x4.mongodb.net/"

/* // This indicates it's a connection to a local database (no authentication involved).
    localhost : This specifies the hostname, which is the local machine in this case.
    :27017 : This is the default port number used by MongoDB.
    /CRM : This might be the database name within the MongoDB server, named "CRM" in this case.*/
}