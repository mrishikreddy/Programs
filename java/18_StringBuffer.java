class out
{
    public static void main(String args[])
    {
        StringBuffer sb = new StringBuffer("Resoulte");
        
        System.out.println("capactiy of SB is (default 16bytes) = "+sb.capacity());
        System.out.println("length of SB is = "+sb.length());
    }
}