import "server-only";

export const serverSideFunction = ()=>{

    console.log(
        `use multiple libraries
         use evironment variables
         interact with database
         process cofidential information
        `)
    return "server result"
};